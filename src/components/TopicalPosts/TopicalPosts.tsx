import { FC } from 'react';

import { GridElementWrapper } from '../UI/GridElementWrapper';

const TopicalPosts: FC = () => {
  return (
    <GridElementWrapper> Topical posts </GridElementWrapper>
  )
}

export default TopicalPosts;
